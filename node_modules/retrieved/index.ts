const createRetrieved = <T>(fn: () => T) => {
  const key = Symbol();

  let value: typeof key | T = key;

  return () => {
    if (value === key) {
      value = fn();
    }

    return value;
  };
};

export { createRetrieved };
export default createRetrieved;
