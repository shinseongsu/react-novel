# Create Retrieved

## Usage

```ts
import { createRetrieved } from "retrieved";

declare function getBrowser(): "chrome" | "firefox" | "safari";

const detectBrowser = createRetrieved(getBrowser);

const browser = detectBrowser();

browser(); // getBrowser() is ran
browser(); // getBrowser() is not ran
```
