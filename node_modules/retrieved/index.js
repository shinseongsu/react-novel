const createRetrieved = (fn) => {
  const key = Symbol();
  let value = key;
  return () => {
    if (value === key) {
      value = fn();
    }
    return value;
  };
};
export { createRetrieved };
export default createRetrieved;
