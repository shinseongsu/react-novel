import type { ValidAction, Character, Lang, CharactersData } from "@novely/core";
type ShowArbitraryCharacterParams<$Characters extends CharactersData<Record<string, Character<Lang>>>, $Character extends keyof $Characters> = {
    id: $Character & string;
    emotion: $Characters[$Character]['emotions'][number] & string;
    extendWith: () => string | string[];
    classes?: string;
    style?: string;
};
declare const createShowArbitraryCharacterAction: <$Characters extends CharactersData<Record<string, Character>>>() => <$Character extends keyof $Characters>(params: ShowArbitraryCharacterParams<$Characters, $Character>) => ValidAction;
export { createShowArbitraryCharacterAction };
